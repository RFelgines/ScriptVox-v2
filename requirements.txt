# ─── ScriptVox — pinned dependencies (major.minor) ───────────────────────────
# Not installed yet. Run: pip install -r requirements.txt
#
# Compatibility note:
#   SQLModel 0.0.38 requires SQLAlchemy 2.x and supports Pydantic V2 natively
#   (Pydantic V2 compat introduced in SQLModel 0.0.14).
#   FastAPI 0.136.x requires Pydantic V2.
#   google-genai replaces the deprecated google-generativeai package.

# ─── API framework ───────────────────────────────────────────────────────────
fastapi~=0.136.0
uvicorn[standard]~=0.34.0

# ─── Database ─────────────────────────────────────────────────────────────────
sqlmodel~=0.0.38
sqlalchemy~=2.0.0

# ─── Validation ───────────────────────────────────────────────────────────────
pydantic~=2.11.0

# ─── Task queue ───────────────────────────────────────────────────────────────
huey~=2.5.0

# ─── HTTP client ──────────────────────────────────────────────────────────────
httpx~=0.28.0

# ─── LLM providers ───────────────────────────────────────────────────────────
ollama~=0.4.0
google-genai~=2.8.0

# ─── Configuration ────────────────────────────────────────────────────────────
python-dotenv~=1.0.0

# ─── TTS providers ───────────────────────────────────────────────────────────
# Piper (local TTS) is invoked as a standalone binary via subprocess, NOT as a
# pip package: piper-tts depends on piper-phonemize, which ships no Windows wheel.
# Download the binary from github.com/rhasspy/piper/releases and set
# PIPER_BINARY_PATH in .env. Licence: GPL-3.0 (OHF-Voice/piper1-gpl).
# ElevenLabs (cloud TTS) uses httpx directly — no extra dependency.
edge-tts~=6.1        # EdgeTTS provider — free, no API key; pulls aiohttp transitively
miniaudio~=1.2       # MP3->PCM decoder for EdgeTTS output; no native lib / no binary required

# ─── Audio encoding ───────────────────────────────────────────────────────────
lameenc~=1.8         # WAV->MP3 encoder (pure Python, no native lib / no binary required)

# ─── EPUB parsing ─────────────────────────────────────────────────────────────
ebooklib~=0.18
beautifulsoup4~=4.12

# ─── File upload (required by FastAPI Form/File) ──────────────────────────────
python-multipart~=0.0.20
