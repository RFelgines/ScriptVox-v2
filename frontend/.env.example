# Base URL of the ScriptVox API the browser calls.
# Defaults to http://localhost:8000 in code if this file / variable is absent,
# so copying it to .env.local is only needed when the API runs elsewhere.
NEXT_PUBLIC_API_URL=http://localhost:8000
